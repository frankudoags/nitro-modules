///
/// BiometricsError.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/NitroHash.hpp>)
#include <NitroModules/NitroHash.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::nitrobiometrics {

  /**
   * An enum which can be represented as a JavaScript union (BiometricsError).
   */
  enum class BiometricsError {
    USER_CANCEL      SWIFT_NAME(userCancel) = 0,
    SYSTEM_CANCEL      SWIFT_NAME(systemCancel) = 1,
    NOT_AVAILABLE      SWIFT_NAME(notAvailable) = 2,
    NOT_ENROLLED      SWIFT_NAME(notEnrolled) = 3,
    LOCKOUT      SWIFT_NAME(lockout) = 4,
    AUTHENTICATION_FAILED      SWIFT_NAME(authenticationFailed) = 5,
    UNKNOWN      SWIFT_NAME(unknown) = 6,
  } CLOSED_ENUM;

} // namespace margelo::nitro::nitrobiometrics

namespace margelo::nitro {

  // C++ BiometricsError <> JS BiometricsError (union)
  template <>
  struct JSIConverter<margelo::nitro::nitrobiometrics::BiometricsError> final {
    static inline margelo::nitro::nitrobiometrics::BiometricsError fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, arg);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("user_cancel"): return margelo::nitro::nitrobiometrics::BiometricsError::USER_CANCEL;
        case hashString("system_cancel"): return margelo::nitro::nitrobiometrics::BiometricsError::SYSTEM_CANCEL;
        case hashString("not_available"): return margelo::nitro::nitrobiometrics::BiometricsError::NOT_AVAILABLE;
        case hashString("not_enrolled"): return margelo::nitro::nitrobiometrics::BiometricsError::NOT_ENROLLED;
        case hashString("lockout"): return margelo::nitro::nitrobiometrics::BiometricsError::LOCKOUT;
        case hashString("authentication_failed"): return margelo::nitro::nitrobiometrics::BiometricsError::AUTHENTICATION_FAILED;
        case hashString("unknown"): return margelo::nitro::nitrobiometrics::BiometricsError::UNKNOWN;
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert \"" + unionValue + "\" to enum BiometricsError - invalid value!");
      }
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, margelo::nitro::nitrobiometrics::BiometricsError arg) {
      switch (arg) {
        case margelo::nitro::nitrobiometrics::BiometricsError::USER_CANCEL: return JSIConverter<std::string>::toJSI(runtime, "user_cancel");
        case margelo::nitro::nitrobiometrics::BiometricsError::SYSTEM_CANCEL: return JSIConverter<std::string>::toJSI(runtime, "system_cancel");
        case margelo::nitro::nitrobiometrics::BiometricsError::NOT_AVAILABLE: return JSIConverter<std::string>::toJSI(runtime, "not_available");
        case margelo::nitro::nitrobiometrics::BiometricsError::NOT_ENROLLED: return JSIConverter<std::string>::toJSI(runtime, "not_enrolled");
        case margelo::nitro::nitrobiometrics::BiometricsError::LOCKOUT: return JSIConverter<std::string>::toJSI(runtime, "lockout");
        case margelo::nitro::nitrobiometrics::BiometricsError::AUTHENTICATION_FAILED: return JSIConverter<std::string>::toJSI(runtime, "authentication_failed");
        case margelo::nitro::nitrobiometrics::BiometricsError::UNKNOWN: return JSIConverter<std::string>::toJSI(runtime, "unknown");
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert BiometricsError to JS - invalid value: "
                                    + std::to_string(static_cast<int>(arg)) + "!");
      }
    }
    static inline bool canConvert(jsi::Runtime& runtime, const jsi::Value& value) {
      if (!value.isString()) {
        return false;
      }
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, value);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("user_cancel"):
        case hashString("system_cancel"):
        case hashString("not_available"):
        case hashString("not_enrolled"):
        case hashString("lockout"):
        case hashString("authentication_failed"):
        case hashString("unknown"):
          return true;
        default:
          return false;
      }
    }
  };

} // namespace margelo::nitro
