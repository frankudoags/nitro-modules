///
/// BiometricsPermissionStatus.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/NitroHash.hpp>)
#include <NitroModules/NitroHash.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::nitrobiometrics {

  /**
   * An enum which can be represented as a JavaScript union (BiometricsPermissionStatus).
   */
  enum class BiometricsPermissionStatus {
    GRANTED      SWIFT_NAME(granted) = 0,
    DENIED      SWIFT_NAME(denied) = 1,
    UNDETERMINED      SWIFT_NAME(undetermined) = 2,
  } CLOSED_ENUM;

} // namespace margelo::nitro::nitrobiometrics

namespace margelo::nitro {

  // C++ BiometricsPermissionStatus <> JS BiometricsPermissionStatus (union)
  template <>
  struct JSIConverter<margelo::nitro::nitrobiometrics::BiometricsPermissionStatus> final {
    static inline margelo::nitro::nitrobiometrics::BiometricsPermissionStatus fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, arg);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("granted"): return margelo::nitro::nitrobiometrics::BiometricsPermissionStatus::GRANTED;
        case hashString("denied"): return margelo::nitro::nitrobiometrics::BiometricsPermissionStatus::DENIED;
        case hashString("undetermined"): return margelo::nitro::nitrobiometrics::BiometricsPermissionStatus::UNDETERMINED;
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert \"" + unionValue + "\" to enum BiometricsPermissionStatus - invalid value!");
      }
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, margelo::nitro::nitrobiometrics::BiometricsPermissionStatus arg) {
      switch (arg) {
        case margelo::nitro::nitrobiometrics::BiometricsPermissionStatus::GRANTED: return JSIConverter<std::string>::toJSI(runtime, "granted");
        case margelo::nitro::nitrobiometrics::BiometricsPermissionStatus::DENIED: return JSIConverter<std::string>::toJSI(runtime, "denied");
        case margelo::nitro::nitrobiometrics::BiometricsPermissionStatus::UNDETERMINED: return JSIConverter<std::string>::toJSI(runtime, "undetermined");
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert BiometricsPermissionStatus to JS - invalid value: "
                                    + std::to_string(static_cast<int>(arg)) + "!");
      }
    }
    static inline bool canConvert(jsi::Runtime& runtime, const jsi::Value& value) {
      if (!value.isString()) {
        return false;
      }
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, value);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("granted"):
        case hashString("denied"):
        case hashString("undetermined"):
          return true;
        default:
          return false;
      }
    }
  };

} // namespace margelo::nitro
