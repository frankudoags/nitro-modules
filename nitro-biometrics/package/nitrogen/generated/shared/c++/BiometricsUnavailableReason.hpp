///
/// BiometricsUnavailableReason.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/NitroHash.hpp>)
#include <NitroModules/NitroHash.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::nitrobiometrics {

  /**
   * An enum which can be represented as a JavaScript union (BiometricsUnavailableReason).
   */
  enum class BiometricsUnavailableReason {
    NOT_SUPPORTED      SWIFT_NAME(notSupported) = 0,
    NOT_ENROLLED      SWIFT_NAME(notEnrolled) = 1,
    NOT_DETERMINED      SWIFT_NAME(notDetermined) = 2,
    DENIED      SWIFT_NAME(denied) = 3,
    LOCKED_OUT      SWIFT_NAME(lockedOut) = 4,
    PASSCODE_NOT_SET      SWIFT_NAME(passcodeNotSet) = 5,
  } CLOSED_ENUM;

} // namespace margelo::nitro::nitrobiometrics

namespace margelo::nitro {

  // C++ BiometricsUnavailableReason <> JS BiometricsUnavailableReason (union)
  template <>
  struct JSIConverter<margelo::nitro::nitrobiometrics::BiometricsUnavailableReason> final {
    static inline margelo::nitro::nitrobiometrics::BiometricsUnavailableReason fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, arg);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("NOT_SUPPORTED"): return margelo::nitro::nitrobiometrics::BiometricsUnavailableReason::NOT_SUPPORTED;
        case hashString("NOT_ENROLLED"): return margelo::nitro::nitrobiometrics::BiometricsUnavailableReason::NOT_ENROLLED;
        case hashString("NOT_DETERMINED"): return margelo::nitro::nitrobiometrics::BiometricsUnavailableReason::NOT_DETERMINED;
        case hashString("DENIED"): return margelo::nitro::nitrobiometrics::BiometricsUnavailableReason::DENIED;
        case hashString("LOCKED_OUT"): return margelo::nitro::nitrobiometrics::BiometricsUnavailableReason::LOCKED_OUT;
        case hashString("PASSCODE_NOT_SET"): return margelo::nitro::nitrobiometrics::BiometricsUnavailableReason::PASSCODE_NOT_SET;
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert \"" + unionValue + "\" to enum BiometricsUnavailableReason - invalid value!");
      }
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, margelo::nitro::nitrobiometrics::BiometricsUnavailableReason arg) {
      switch (arg) {
        case margelo::nitro::nitrobiometrics::BiometricsUnavailableReason::NOT_SUPPORTED: return JSIConverter<std::string>::toJSI(runtime, "NOT_SUPPORTED");
        case margelo::nitro::nitrobiometrics::BiometricsUnavailableReason::NOT_ENROLLED: return JSIConverter<std::string>::toJSI(runtime, "NOT_ENROLLED");
        case margelo::nitro::nitrobiometrics::BiometricsUnavailableReason::NOT_DETERMINED: return JSIConverter<std::string>::toJSI(runtime, "NOT_DETERMINED");
        case margelo::nitro::nitrobiometrics::BiometricsUnavailableReason::DENIED: return JSIConverter<std::string>::toJSI(runtime, "DENIED");
        case margelo::nitro::nitrobiometrics::BiometricsUnavailableReason::LOCKED_OUT: return JSIConverter<std::string>::toJSI(runtime, "LOCKED_OUT");
        case margelo::nitro::nitrobiometrics::BiometricsUnavailableReason::PASSCODE_NOT_SET: return JSIConverter<std::string>::toJSI(runtime, "PASSCODE_NOT_SET");
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert BiometricsUnavailableReason to JS - invalid value: "
                                    + std::to_string(static_cast<int>(arg)) + "!");
      }
    }
    static inline bool canConvert(jsi::Runtime& runtime, const jsi::Value& value) {
      if (!value.isString()) {
        return false;
      }
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, value);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("NOT_SUPPORTED"):
        case hashString("NOT_ENROLLED"):
        case hashString("NOT_DETERMINED"):
        case hashString("DENIED"):
        case hashString("LOCKED_OUT"):
        case hashString("PASSCODE_NOT_SET"):
          return true;
        default:
          return false;
      }
    }
  };

} // namespace margelo::nitro
