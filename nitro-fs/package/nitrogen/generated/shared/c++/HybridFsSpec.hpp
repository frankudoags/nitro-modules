///
/// HybridFsSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `FileEntry` to properly resolve imports.
namespace margelo::nitro::nitrofs { struct FileEntry; }

#include <string>
#include <NitroModules/Promise.hpp>
#include "FileEntry.hpp"
#include <vector>

namespace margelo::nitro::nitrofs {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `Fs`
   * Inherit this class to create instances of `HybridFsSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridFs: public HybridFsSpec {
   * public:
   *   HybridFs(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridFsSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridFsSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridFsSpec() override = default;

    public:
      // Properties
      virtual std::string getDocumentsDirectory() = 0;
      virtual std::string getCacheDirectory() = 0;
      virtual std::string getTempDirectory() = 0;

    public:
      // Methods
      virtual std::shared_ptr<Promise<std::string>> readFile(const std::string& path) = 0;
      virtual std::shared_ptr<Promise<void>> writeFile(const std::string& path, const std::string& content) = 0;
      virtual std::shared_ptr<Promise<void>> deleteFile(const std::string& path) = 0;
      virtual std::shared_ptr<Promise<void>> mkdir(const std::string& path) = 0;
      virtual std::shared_ptr<Promise<std::vector<FileEntry>>> readDir(const std::string& path) = 0;
      virtual std::shared_ptr<Promise<bool>> exists(const std::string& path) = 0;
      virtual std::shared_ptr<Promise<void>> move(const std::string& srcPath, const std::string& destPath) = 0;
      virtual std::shared_ptr<Promise<void>> copy(const std::string& srcPath, const std::string& destPath) = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "Fs";
  };

} // namespace margelo::nitro::nitrofs
