///
/// HybridBatterySpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `BatteryInfo` to properly resolve imports.
namespace margelo::nitro::nitrobattery { struct BatteryInfo; }

#include "BatteryInfo.hpp"
#include <functional>

namespace margelo::nitro::nitrobattery {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `Battery`
   * Inherit this class to create instances of `HybridBatterySpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridBattery: public HybridBatterySpec {
   * public:
   *   HybridBattery(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridBatterySpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridBatterySpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridBatterySpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual BatteryInfo getBatteryInfo() = 0;
      virtual double subscribe(const std::function<void(const BatteryInfo& /* info */)>& callback) = 0;
      virtual void unsubscribe(double id) = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "Battery";
  };

} // namespace margelo::nitro::nitrobattery
