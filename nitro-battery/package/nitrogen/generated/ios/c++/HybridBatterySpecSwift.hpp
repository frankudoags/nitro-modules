///
/// HybridBatterySpecSwift.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#include "HybridBatterySpec.hpp"

// Forward declaration of `HybridBatterySpec_cxx` to properly resolve imports.
namespace NitroBattery { class HybridBatterySpec_cxx; }

// Forward declaration of `BatteryInfo` to properly resolve imports.
namespace margelo::nitro::nitrobattery { struct BatteryInfo; }
// Forward declaration of `BatteryStateResult` to properly resolve imports.
namespace margelo::nitro::nitrobattery { enum class BatteryStateResult; }

#include "BatteryInfo.hpp"
#include <NitroModules/Null.hpp>
#include <variant>
#include <optional>
#include "BatteryStateResult.hpp"
#include <functional>

#include "NitroBattery-Swift-Cxx-Umbrella.hpp"

namespace margelo::nitro::nitrobattery {

  /**
   * The C++ part of HybridBatterySpec_cxx.swift.
   *
   * HybridBatterySpecSwift (C++) accesses HybridBatterySpec_cxx (Swift), and might
   * contain some additional bridging code for C++ <> Swift interop.
   *
   * Since this obviously introduces an overhead, I hope at some point in
   * the future, HybridBatterySpec_cxx can directly inherit from the C++ class HybridBatterySpec
   * to simplify the whole structure and memory management.
   */
  class HybridBatterySpecSwift: public virtual HybridBatterySpec {
  public:
    // Constructor from a Swift instance
    explicit HybridBatterySpecSwift(const NitroBattery::HybridBatterySpec_cxx& swiftPart):
      HybridObject(HybridBatterySpec::TAG),
      _swiftPart(swiftPart) { }

  public:
    // Get the Swift part
    inline NitroBattery::HybridBatterySpec_cxx& getSwiftPart() noexcept {
      return _swiftPart;
    }

  public:
    inline size_t getExternalMemorySize() noexcept override {
      return _swiftPart.getMemorySize();
    }
    bool equals(const std::shared_ptr<HybridObject>& other) override {
      if (auto otherCast = std::dynamic_pointer_cast<HybridBatterySpecSwift>(other)) {
        return _swiftPart.equals(otherCast->_swiftPart);
      }
      return false;
    }
    void dispose() noexcept override {
      _swiftPart.dispose();
    }
    std::string toString() override {
      return _swiftPart.toString();
    }

  public:
    // Properties
    

  public:
    // Methods
    inline BatteryInfo getBatteryInfo() override {
      auto __result = _swiftPart.getBatteryInfo();
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
      auto __value = std::move(__result.value());
      return __value;
    }
    inline double subscribe(const std::function<void(const BatteryInfo& /* info */)>& callback) override {
      auto __result = _swiftPart.subscribe(callback);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
      auto __value = std::move(__result.value());
      return __value;
    }
    inline void unsubscribe(double id) override {
      auto __result = _swiftPart.unsubscribe(std::forward<decltype(id)>(id));
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }

  private:
    NitroBattery::HybridBatterySpec_cxx _swiftPart;
  };

} // namespace margelo::nitro::nitrobattery
